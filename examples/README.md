(Existing content...)

## Running Locally

Once you have the repo, you can run leaps against your own data:

```bash
python cli/council_cli.py --pack path/to/your-export.tar.gz
```

This keeps everything on your machine while still grounding in live literature via the S2 client.